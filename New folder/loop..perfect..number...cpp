//write a c program to check whether a input number is perfect number of not.

#include<stdio.h>
int main()
{
	int number,divisors,perfect_number,sum=0;
	printf("enter the number  :");
	scanf("%d",&number);
    while(number=divisors)
    {
    	divisors=1+2+3;
    	number=divisors % 10;
    	perfect_number=divisors + sum;
    	
	}
	printf("perfect number is=%d",perfect_number);
}
